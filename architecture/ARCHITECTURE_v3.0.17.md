# 天一阁架构文档 v3.0.17

**版本**: v3.0.17
**日期**: 2026-04-08
**主题**: 监控/限流/缓存 API 实现 + 前后端联调 + 端到端测试

---

## 📋 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v3.0.17 | 2026-04-08 | 监控/限流/缓存 API 实现 + 前后端联调 + 端到端测试 |
| v3.0.16 | 2026-04-05 | 实现代码开发 + 前端 UI 组件开发 + 单元测试与集成测试 |
| v3.0.15 | 2026-04-04 | 监控架构 + 限流架构 + 多级缓存架构 + 性能优化架构 + 搜索增强架构 + LLM 成本追踪架构 |
| v3.0.14 | 2026-04-03 | 推荐架构 + 版本对比架构 + 工作流引擎架构 + 分析架构 + i18n 架构 |

---

## 🏗️ 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              前端层 (React + TypeScript)                                │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  监控仪表盘  │  限流配置  │  缓存管理  │  Trace 查看器  │  日志查询  │  配额管理  │
│  Dashboard  │  Config   │  Cache UI  │  Trace Viewer │  Log Query│  Quota Mgr │
│  - Charts   │  - Quota  │  - Stats   │  - Spans      │  - Search │  - Rules   │
│  - Services │  - Rules  │  - Warmup  │  - Timeline   │  - Filter │  - Usage   │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              API 层 (FastAPI)                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  /metrics   │  /dashboard │  /traces   │  /logs      │  /rate-limit │  /cache    │
│  GET /      │  GET /over  │  GET /:id  │  GET /      │  GET /status │  GET /stats│
│  GET /sum   │  GET /http  │  GET /list │  GET /agg   │  POST /check │  POST /inv │
│  GET /health│  GET /llm   │  GET /svc  │             │  CRUD /quota │  POST /warm│
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              服务层 (Services)                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  MetricsCollector  │  TracingManager   │  StructuredLogger    │  TokenBucket      │
│  - Prometheus       │  - OpenTelemetry │  - JSON Formatter     │  - Redis Lua      │
│  - Counter/Gauge   │  - Span/Trace    │  - Loki Sink         │  - Quota Check    │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  MultiLevelCache    │  QuotaManager     │  LlmCostTracker      │  SearchService    │
│  - L1 Memory LRU   │  - Daily/Monthly │  - Usage Record      │  - FacetedQuery   │
│  - L2 Redis        │  - Budget Alert   │  - Cost Calc         │  - Aggregation    │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  CacheWarmupService │  RuleManager      │  DashboardAggregator │  TraceQuerySvc    │
│  - Background Tasks │  - CRUD Rules     │  - Time Series       │  - Service List   │
│  - Progress Track  │  - Enable/Disable │  - Percentiles        │  - Dependencies   │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                       │
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              数据层                                                      │
├─────────────────────────────────────────────────────────────────────────────────────┤
│  PostgreSQL   │  Redis            │  Prometheus   │  Jaeger    │  Loki           │
│  (PgBouncer) │  (Sentinel/Cluster)│  (Metrics)    │  (Traces)  │  (Logs)         │
│  - quotas    │  - cache          │  - scrape     │  - otlp    │  - log storage  │
│  - rules     │  - rate_limit    │  - storage    │  - storage │  - queries      │
│  - usage     │  - sessions       │               │  - ui      │                 │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 一、项目结构

```
skyone-shuge/
├── src/
│   └── skyone_shuge/
│       ├── __init__.py
│       ├── main.py                    # FastAPI 应用入口
│       ├── core/
│       │   ├── __init__.py
│       │   ├── config.py              # 配置管理
│       │   ├── database.py            # 数据库连接池管理
│       │   ├── auth.py               # 认证
│       │   ├── celery_app.py          # Celery 配置
│       │   └── connection_manager.py  # WebSocket 连接管理
│       ├── api/
│       │   ├── __init__.py
│       │   ├── v1/
│       │   │   ├── __init__.py
│       │   │   ├── router.py          # API 路由汇总
│       │   │   ├── documents.py        # 文档 API
│       │   │   ├── search.py          # 搜索 API
│       │   │   ├── auth.py            # 认证 API
│       │   │   ├── metrics.py         # Prometheus 指标 API
│       │   │   ├── traces.py          # OpenTelemetry 追踪 API
│       │   │   ├── logs.py            # 日志查询 API
│       │   │   ├── dashboard.py       # 监控仪表盘 API
│       │   │   ├── rate_limit.py      # 限流状态 API
│       │   │   ├── quota.py           # 配额管理 API
│       │   │   ├── rate_limit_rules.py# 限流规则 API
│       │   │   ├── cache.py           # 缓存管理 API
│       │   │   ├── cache_invalidate.py# 缓存清理 API
│       │   │   ├── cache_warmup.py    # 缓存预热 API
│       │   │   └── costs.py           # 成本 API
│       │   └── deps.py                # 依赖注入
│       ├── models/                     # SQLAlchemy 模型
│       │   ├── __init__.py
│       │   ├── user.py
│       │   ├── document.py
│       │   ├── quota.py               # 配额模型
│       │   ├── rate_limit_rule.py     # 限流规则模型
│       │   └── llm_usage.py           # LLM 使用记录
│       ├── schemas/                    # Pydantic schemas
│       │   ├── __init__.py
│       │   ├── user.py
│       │   ├── document.py
│       │   ├── search.py
│       │   ├── metrics.py
│       │   ├── quota.py
│       │   └── cache.py
│       ├── services/                   # 业务逻辑
│       │   ├── __init__.py
│       │   ├── document_service.py
│       │   ├── search_service.py
│       │   ├── quota_manager.py       # 配额管理服务
│       │   ├── llm_cost_tracker.py    # LLM 成本追踪
│       │   ├── cache_service.py       # 缓存服务
│       │   └── warmup_service.py      # 预热服务
│       ├── ml/                         # 机器学习
│       │   ├── __init__.py
│       │   ├── embedding.py
│       │   ├── vector_db.py
│       │   ├── llm.py
│       │   └── rag.py
│       ├── tasks/                      # Celery 任务
│       │   ├── __init__.py
│       │   ├── document_tasks.py
│       │   ├── embedding_tasks.py
│       │   ├── index_tasks.py
│       │   └── notification_tasks.py
│       ├── cache/                      # 缓存模块
│       │   ├── __init__.py
│       │   └── multi_level.py         # 多级缓存
│       ├── monitoring/                 # 监控模块
│       │   ├── __init__.py
│       │   ├── metrics.py             # Prometheus 指标
│       │   ├── tracing.py             # OpenTelemetry 追踪
│       │   └── logging.py             # 结构化日志
│       ├── middleware/                  # 中间件
│       │   ├── __init__.py
│       │   ├── rate_limiter.py        # 限流中间件
│       │   └── cors.py
│       ├── search/                      # 搜索模块
│       │   ├── __init__.py
│       │   └── elasticsearch_client.py # ES 客户端
│       └── cli/
│           ├── __init__.py
│           └── worker.py
├── frontend/
│   └── src/
│       ├── components/
│       │   ├── monitoring/
│       │   │   ├── MetricsChart.tsx
│       │   │   ├── TraceViewer.tsx
│       │   │   ├── LogExplorer.tsx
│       │   │   └── ServiceStatus.tsx
│       │   ├── rate-limit/
│       │   │   ├── QuotaManager.tsx
│       │   │   └── RuleEditor.tsx
│       │   └── cache/
│       │       ├── CacheStats.tsx
│       │       ├── CacheInvalidate.tsx
│       │       └── CacheWarmup.tsx
│       ├── pages/
│       │   ├── Dashboard.tsx
│       │   ├── MonitoringDashboard.tsx
│       │   ├── RateLimitConfig.tsx
│       │   ├── CacheManagement.tsx
│       │   ├── SearchConsole.tsx
│       │   └── CostDashboard.tsx
│       ├── hooks/
│       │   ├── useMetrics.ts
│       │   ├── useTraces.ts
│       │   ├── useCache.ts
│       │   └── useQuota.ts
│       ├── services/
│       │   ├── api/
│       │   │   ├── client.ts
│       │   │   ├── monitoring.ts
│       │   │   ├── rateLimit.ts
│       │   │   └── cache.ts
│       │   └── websocket.ts
│       └── App.tsx
├── tests/
│   ├── __init__.py
│   ├── conftest.py                     # pytest 配置
│   ├── unit/
│   │   ├── __init__.py
│   │   ├── test_rate_limiter.py
│   │   ├── test_llm_cost_tracker.py
│   │   ├── test_multi_level_cache.py
│   │   └── test_token_bucket.py
│   ├── integration/
│   │   ├── __init__.py
│   │   ├── test_api_integration.py
│   │   ├── test_database_integration.py
│   │   └── test_redis_integration.py
│   └── e2e/
│       ├── __init__.py
│       ├── test_user_flows.py          # 用户流程 E2E
│       └── test_websocket.py           # WebSocket 测试
├── scripts/
│   ├── init_db.py
│   ├── send_code_email.py
│   └── run_tests.sh
├── docker-compose.yml
├── Dockerfile
├── pyproject.toml
├── pytest.ini
└── README.md
```

---

## 🔌 二、API 端点架构

### 2.1 API 端点分组

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              API 端点分组架构                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                          监控指标组 (/metrics, /dashboard)                      │  │
│  │                                                                            │  │
│  │   GET /metrics                    → Prometheus 格式指标                     │  │
│  │   GET /metrics/summary            → JSON 格式指标摘要                        │  │
│  │   GET /metrics/health             → 监控子系统健康状态                        │  │
│  │                                                                            │  │
│  │   GET /dashboard/overview         → 仪表盘概览数据                           │  │
│  │   GET /dashboard/http-requests     → HTTP 请求详细数据                       │  │
│  │   GET /dashboard/llm-usage         → LLM 使用详细数据                         │  │
│  │   GET /dashboard/cache-dashboard  → 缓存仪表盘数据                          │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                          链路追踪组 (/traces)                                  │  │
│  │                                                                            │  │
│  │   GET /traces/{trace_id}           → 获取指定 Trace                          │  │
│  │   GET /traces/list                 → 查询 Trace 列表                          │  │
│  │   GET /traces/services             → 服务列表                                │  │
│  │   GET /traces/dependencies         → 服务依赖关系图                          │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                          日志查询组 (/logs)                                    │  │
│  │                                                                            │  │
│  │   GET /logs                       → 查询日志                                 │  │
│  │   GET /logs/aggregations          → 日志聚合统计                             │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                          限流管理组 (/rate-limit)                              │  │
│  │                                                                            │  │
│  │   GET  /rate-limit/status         → 限流状态查询                             │  │
│  │   GET  /rate-limit/status/batch   → 批量限流状态                             │  │
│  │                                                                            │  │
│  │   CRUD /rate-limit/quota/quotas   → 配额管理                                │  │
│  │   GET  /rate-limit/quota/quotas/{id}/usage → 配额使用情况                     │  │
│  │   POST /rate-limit/quota/quotas/{id}/pause  → 暂停配额                       │  │
│  │   POST /rate-limit/quota/quotas/{id}/resume → 恢复配额                       │  │
│  │                                                                            │  │
│  │   CRUD /rate-limit/rules/rules    → 限流规则管理                            │  │
│  │   POST /rate-limit/rules/rules/{id}/enable   → 启用规则                      │  │
│  │   POST /rate-limit/rules/rules/{id}/disable  → 禁用规则                      │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                          缓存管理组 (/cache)                                   │  │
│  │                                                                            │  │
│  │   GET  /cache/stats              → 缓存统计                                  │  │
│  │   GET  /cache/stats/memory       → 内存缓存统计                              │  │
│  │   GET  /cache/stats/redis        → Redis 缓存统计                           │  │
│  │   GET  /cache/keys               → 缓存键列表                                │  │
│  │   GET  /cache/keys/{key}         → 键详情                                    │  │
│  │                                                                            │  │
│  │   POST /cache/invalidate          → 清理缓存                                 │  │
│  │   POST /cache/invalidate/user/{id}   → 清理用户缓存                          │  │
│  │   POST /cache/invalidate/document/{id} → 清理文档缓存                       │  │
│  │   POST /cache/invalidate/search     → 清理搜索缓存                          │  │
│  │   POST /cache/flush              → 清空所有缓存（危险）                       │  │
│  │                                                                            │  │
│  │   POST /cache/warmup             → 启动缓存预热                              │  │
│  │   GET  /cache/warmup/{task_id}   → 预热任务状态                              │  │
│  │   GET  /cache/warmup             → 预热任务列表                              │  │
│  │                                                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 API 请求/响应流程

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              API 请求/响应流程                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  客户端请求                                                                    响应  │
│     │                                                                      │        │
│     ▼                                                                      ▼        │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         1. 请求入口                                         │  │
│  │   POST /rate-limit/quota/quotas                                           │  │
│  │   Content-Type: application/json                                         │  │
│  │   Authorization: Bearer {token}                                            │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         2. 中间件处理                                         │  │
│  │   ├─ CORS Middleware                                                       │  │
│  │   ├─ Auth Middleware (JWT 验证)                                            │  │
│  │   ├─ Rate Limit Middleware (检查限流)                                       │  │
│  │   └─ Metrics Middleware (记录指标)                                          │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         3. 路由匹配                                           │  │
│  │   /rate-limit/quota/quotas                                                │  │
│  │   → router.post("/quotas")                                                 │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         4. 依赖注入                                           │  │
│  │   async def create_quota(                                                   │  │
│  │       request: QuotaCreateRequest,                                         │  │
│  │       db: Session = Depends(get_db),    # 数据库会话                         │  │
│  │       current_user: User = Depends(get_current_user),  # 当前用户            │  │
│  │   ):                                                                         │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         5. 业务逻辑                                           │  │
│  │   ├─ 验证请求数据 (Pydantic)                                                 │  │
│  │   ├─ 检查配额是否已存在                                                       │  │
│  │   ├─ 创建配额记录                                                             │  │
│  │   └─ 初始化 Redis 缓存                                                       │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         6. 响应处理                                           │  │
│  │   ├─ 记录成功日志                                                             │  │
│  │   ├─ 更新监控指标                                                             │  │
│  │   └─ 返回 JSON 响应                                                          │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         7. 响应返回                                           │  │
│  │   201 Created                                                               │  │
│  │   {                                                                         │  │
│  │     "id": "quota_xxx",                                                      │  │
│  │     "name": "test_quota",                                                  │  │
│  │     "status": "active",                                                    │  │
│  │     ...                                                                     │  │
│  │   }                                                                         │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 三、核心模块 API 实现

### 3.1 监控 API 架构

#### 3.1.1 Metrics API 流程

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              Metrics API 请求流程                                       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  GET /metrics                                                               200 OK  │
│     │                                                                      │        │
│     ▼                                                                      ▼        │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                    MetricsCollector.get_metrics()                           │  │
│  │                                                                            │  │
│  │   ┌────────────────────────────────────────────────────────────────────┐  │  │
│  │   │  Registry._names_to_collectors                                      │  │  │
│  │   │  ├── skyone_http_requests_total (Counter)                           │  │  │
│  │   │  ├── skyone_llm_tokens_total (Counter)                             │  │  │
│  │   │  ├── skyone_http_request_duration_seconds (Histogram)             │  │  │
│  │   │  ├── skyone_cache_hits_total (Counter)                             │  │  │
│  │   │  └── ...                                                           │  │  │
│  │   └────────────────────────────────────────────────────────────────────┘  │  │
│  │                                      │                                      │  │
│  │                                      ▼                                      │  │
│  │   ┌────────────────────────────────────────────────────────────────────┐  │  │
│  │   │  prometheus_client.generate_latest(registry)                       │  │  │
│  │   │                                                                    │  │  │
│  │   │  输出格式:                                                         │  │  │
│  │   │  # HELP skyone_http_requests_total Total HTTP requests           │  │  │
│  │   │  # TYPE skyone_http_requests_total counter                        │  │  │
│  │   │  skyone_http_requests_total{method="GET",endpoint="/api",status="200"} 1  │  │
│  │   │                                                                    │  │  │
│  │   └────────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                    Prometheus Server (Scrape)                                │  │
│  │   ┌────────────────────────────────────────────────────────────────────┐  │  │
│  │   │  prometheus.yml                                                    │  │  │
│  │   │  scrape_configs:                                                   │  │  │
│  │   │    - job_name: 'skyone-shuge'                                      │  │  │
│  │   │      static_configs:                                               │  │  │
│  │   │        - targets: ['api:8000']                                     │  │  │
│  │   │      metrics_path: '/metrics'                                      │  │  │
│  │   │      scrape_interval: 15s                                          │  │  │
│  │   └────────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                    Grafana Dashboard                                        │  │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                         │  │
│  │   │ Overview    │  │ LLM Cost    │  │ Cache Hit   │                         │  │
│  │   │ Panel       │  │ Panel       │  │ Ratio Panel │                         │  │
│  │   └─────────────┘  └─────────────┘  └─────────────┘                         │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 Dashboard API 聚合流程

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              Dashboard API 数据聚合流程                                  │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  GET /dashboard/overview?period=1h                                    200 OK       │
│     │                                                                      │         │
│     ▼                                                                      ▼         │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      DashboardAggregator                                    │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐ │   │
│  │   │  Parallel Query (asyncio.gather)                                     │ │   │
│  │   │                                                                       │ │   │
│  │   │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────┐  │ │   │
│  │   │  │ HTTP Summary │  │ LLM Usage    │  │ Cache Stats │  │ Errors   │  │ │   │
│  │   │  │ from Redis   │  │ from Redis   │  │ from Cache   │  │ from Log │  │ │   │
│  │   │  │ / DB        │  │ / DB        │  │ Module      │  │ Store    │  │ │   │
│  │   │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └────┬────┘  │ │   │
│  │   │         │                 │                 │                │        │ │   │
│  │   │         └─────────────────┼─────────────────┼────────────────┘        │ │   │
│  │   │                           ▼                                      │ │   │
│  │   │  ┌─────────────────────────────────────────────────────────────┐ │ │   │
│  │   │  │  Aggregation & Transformation                               │ │ │   │
│  │   │  │  - 计算同比/环比                                              │ │ │   │
│  │   │  │  - 计算趋势 (up/down/stable)                                 │ │ │   │
│  │   │  │  - 生成时间序列                                               │ │ │   │
│  │   │  └─────────────────────────────────────────────────────────────┘ │ │   │
│  │   └─────────────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      Response                                                │   │
│  │   {                                                                         │   │
│  │     "period": "1h",                                                         │   │
│  │     "generated_at": "2026-04-08T23:00:00Z",                                │   │
│  │     "http_requests": {                                                     │   │
│  │       "total": 12543,                                                      │   │
│  │       "previous": 10231,                                                   │   │
│  │       "change_percent": 22.6,                                              │   │
│  │       "trend": "up",                                                       │   │
│  │       "timeline": [{"timestamp": "...", "value": 125}]                    │   │
│  │     },                                                                      │   │
│  │     "llm_usage": {...},                                                    │   │
│  │     "cache_performance": {...},                                            │   │
│  │     "error_rate": {...}                                                    │   │
│  │   }                                                                         │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 追踪 API 架构

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              Trace API 查询架构                                        │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  GET /traces/list?service=api&limit=100                               200 OK       │
│     │                                                                      │         │
│     ▼                                                                      ▼         │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      TraceQueryService                                      │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐ │   │
│  │   │  1. Build Query                                                       │ │   │
│  │   │     - service: "api"                                                  │ │   │
│  │   │     - start_time: now - 1h                                            │ │   │
│  │   │     - end_time: now                                                    │ │   │
│  │   │     - limit: 100                                                       │ │   │
│  │   └─────────────────────────────────────────────────────────────────────┘ │   │
│  │                                      │                                      │   │
│  │                                      ▼                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐ │   │
│  │   │  2. Query Jaeger / OTLP Backend                                       │ │   │
│  │   │                                                                       │ │   │
│  │   │     ┌─────────────────────────────────────────────────────────────┐  │ │   │
│  │   │     │  Jaeger Query API                                             │  │ │   │
│  │   │     │  POST /api/traces                                              │  │ │   │
│  │   │     │  {                                                             │  │ │   │
│  │   │     │    "query": {                                                  │  │ │   │
│  │   │     │      "service": "api",                                        │  │ │   │
│  │   │     │      "start": "2026-04-08T22:00:00Z",                         │  │ │   │
│  │   │     │      "end": "2026-04-08T23:00:00Z",                          │  │ │   │
│  │   │     │      "limit": 100                                             │  │ │   │
│  │   │     │    }                                                           │  │ │   │
│  │   │     │  }                                                             │  │ │   │
│  │   │     └─────────────────────────────────────────────────────────────┘  │ │   │
│  │   └─────────────────────────────────────────────────────────────────────┘ │   │
│  │                                      │                                      │   │
│  │                                      ▼                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐ │   │
│  │   │  3. Transform Response                                                │ │   │
│  │   │                                                                       │ │   │
│  │   │  Jaeger Trace ──────────────────────► Our Trace Model                │ │   │
│  │   │  {                                             {                      │ │   │
│  │   │    traceID: "abc",                           traceId: "abc",        │ │   │
│  │   │    spans: [...],        ──────────────►      spans: [...],          │ │   │
│  │   │    duration: 12345                            duration: 12.345        │ │   │
│  │   │  }                                             ...                    │ │   │
│  │   │                                               }                      │ │   │
│  │   └─────────────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│                                      ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      Response                                                │   │
│  │   {                                                                         │   │
│  │     "traces": [                                                             │   │
│  │       {                                                                     │   │
│  │         "traceId": "abc123",                                               │   │
│  │         "spans": [...],                                                    │   │
│  │         "totalSpans": 15,                                                  │   │
│  │         "duration": 1234.5,                                                │   │
│  │         "startTime": 1741564800000,                                        │   │
│  │         "endTime": 1741564801234                                           │   │
│  │       }                                                                     │   │
│  │     ],                                                                      │   │
│  │     "total": 1234,                                                          │   │
│  │     "limit": 100,                                                           │   │
│  │     "offset": 0                                                            │   │
│  │   }                                                                         │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 限流 API 架构

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              限流 API 架构                                             │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      Rate Limiting API 分层                                   │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  限流状态层 (Rate Limit Status)                                      │  │   │
│  │   │  GET /rate-limit/status                                              │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  Redis Hash: ratelimit:user:{id}                              │  │  │   │
│  │   │  │  {                                                             │  │  │   │
│  │   │  │    tokens: 95.5,          # 当前令牌数                          │  │  │   │
│  │   │  │    capacity: 100,         # 桶容量                              │  │  │   │
│  │   │  │    last_update: 1741564800,  # 最后更新时间                     │  │  │   │
│  │   │  │    rate: 1.666           # 每秒补充令牌数 (100/60)              │  │  │   │
│  │   │  │  }                                                             │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  │                                      │                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  配额管理层 (Quota Management)                                       │  │   │
│  │   │  CRUD /rate-limit/quota/quotas                                      │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  PostgreSQL Table: quotas                                     │  │  │   │
│  │   │  │  {                                                             │  │  │   │
│  │   │  │    id: uuid,                                                   │  │  │   │
│  │   │  │    name: "enterprise_quota",                                   │  │  │   │
│  │   │  │    quota_type: "organization",                                 │  │  │   │
│  │   │  │    daily_tokens: 1000000,                                      │  │  │   │
│  │   │  │    daily_cost_usd: 100.0,                                      │  │  │   │
│  │   │  │    monthly_tokens: 10000000,                                  │  │  │   │
│  │   │  │    monthly_cost_usd: 1000.0,                                  │  │  │   │
│  │   │  │    requests_per_minute: 100,                                  │  │  │   │
│  │   │  │    status: "active"                                            │  │  │   │
│  │   │  │  }                                                             │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   │                              │                                      │  │   │
│  │   │                              ▼                                      │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  Redis Hash: quota:llm:user:{id}                              │  │  │   │
│  │   │  │  daily:{date}:{tokens,cost}  ← 实时使用量                      │  │  │   │
│  │   │  │  monthly:{month}:{tokens,cost}                                 │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  │                                      │                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  规则管理层 (Rate Limit Rules)                                       │  │   │
│  │   │  CRUD /rate-limit/rules/rules                                       │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  PostgreSQL Table: rate_limit_rules                           │  │  │   │
│  │   │  │  {                                                             │  │  │   │
│  │   │  │    id: uuid,                                                   │  │  │   │
│  │   │  │    name: "api_default",                                        │  │  │   │
│  │   │  │    rule_type: "endpoint",                                      │  │  │   │
│  │   │  │    target: "/api/v1/search",                                   │  │  │   │
│  │   │  │    rate: 10.0,           # 10 tokens/second                     │  │  │   │
│  │   │  │    capacity: 100,                                               │  │  │   │
│  │   │  │    window_seconds: 60,                                         │  │  │   │
│  │   │  │    enabled: true,                                               │  │  │   │
│  │   │  │    priority: 0                                                 │  │  │   │
│  │   │  │  }                                                             │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.4 缓存 API 架构

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              缓存 API 架构                                             │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      缓存统计 API (Cache Stats)                                 │   │
│  │   GET /cache/stats                                                            │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  MultiLevelCache.get_stats()                                         │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────┐    ┌───────────────────────┐              │  │   │
│  │   │  │   Memory Cache (L1)  │    │   Redis Cache (L2)    │              │  │   │
│  │   │  │                       │    │                       │              │  │   │
│  │   │  │  size: 450            │    │  keys: 12345          │              │  │   │
│  │   │  │  capacity: 1000        │    │  memory: 50MB         │              │  │   │
│  │   │  │  hit_count: 10000      │    │  hit_count: 50000     │              │  │   │
│  │   │  │  miss_count: 500       │    │  miss_count: 2500     │              │  │   │
│  │   │  │  hit_rate: 0.952       │    │  hit_rate: 0.952      │              │  │   │
│  │   │  └───────────────────────┘    └───────────────────────┘              │  │   │
│  │   │                               │                                      │  │   │
│  │   │                               ▼                                      │  │   │
│  │   │  ┌─────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  total_hit_rate = (10000 + 50000) / (10500 + 52500) = 0.91 │  │  │   │
│  │   │  └─────────────────────────────────────────────────────────────┘  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      缓存清理 API (Cache Invalidate)                           │   │
│  │   POST /cache/invalidate                                                     │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  请求体:                                                              │  │   │
│  │   │  { pattern: "user:*" }  或  { keys: ["key1", "key2"] }              │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  │                                      │                                      │   │
│  │                                      ▼                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  Pattern Mode:                                                       │  │   │
│  │   │  redis.scan_iter(match="skyone:cache:user:*") ──► redis.delete(*keys) │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  Memory Cache Invalidation:                                   │  │  │   │
│  │   │  │  for key in list(memory_cache.keys()):                        │  │  │   │
│  │   │  │      if pattern.replace('*', '') in key:                       │  │  │   │
│  │   │  │          del memory_cache[key]                                 │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      缓存预热 API (Cache Warmup)                               │   │
│  │   POST /cache/warmup                                                         │   │
│  │                                                                              │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  WarmupTask {                                                       │  │   │
│  │   │    task_id: "uuid",                                                 │  │   │
│  │   │    target: "all|user|document|search",                             │  │   │
│  │   │    status: "pending|running|completed|failed",                     │  │   │
│  │   │    progress: 0-100,                                                 │  │   │
│  │   │    items_warmed: 0                                                  │  │   │
│  │   │  }                                                                  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  │                                      │                                      │   │
│  │                                      ▼                                      │   │
│  │   ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │   │  Background Task (Celery / asyncio)                                  │  │   │
│  │   │                                                                       │  │   │
│  │   │  ┌───────────────────────────────────────────────────────────────┐  │  │   │
│  │   │  │  1. Hot Users (top 100 by activity)                           │  │  │   │
│  │   │  │     → Load profile from DB → Write to L1/L2                   │  │  │   │
│  │   │  ├───────────────────────────────────────────────────────────────┤  │  │   │
│  │   │  │  2. Hot Documents (top 100 by views)                           │  │  │   │
│  │   │  │     → Load content from DB → Write to L1/L2                   │  │  │   │
│  │   │  ├───────────────────────────────────────────────────────────────┤  │  │   │
│  │   │  │  3. Popular Searches (top 50)                                  │  │  │   │
│  │   │  │     → Execute search → Cache results (short TTL)               │  │  │   │
│  │   │  └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   │                              │                                      │  │   │
│  │   │                              ▼                                      │  │   │
│  │   │  Progress Updates ──► Redis (task:{id}:progress)                  │  │   │
│  │   └─────────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 四、前端组件架构

### 4.1 监控仪表盘组件树

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           监控仪表盘组件结构                                            │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  <MonitoringDashboard />                         监控仪表盘主组件                    │
│  ├── <DashboardHeader />                       仪表盘头部                          │
│  │   ├── <Title>监控仪表盘</Title>                                               │
│  │   └── <PeriodSelector />                    时间范围选择器                      │
│  │       └── <Select value={period}>           1h / 6h / 24h / 7d                 │
│  │                                                                  │
│  ├── <StatsOverview />                         统计概览行                          │
│  │   ├── <StatCard title="总请求数" value={...} />                                    │
│  │   ├── <StatCard title="平均延迟" value={...} />                                   │
│  │   ├── <StatCard title="错误率" value={...} />                                    │
│  │   └── <StatCard title="LLM成本" value={...} />                                   │
│  │                                                                  │
│  ├── <ChartsSection />                        图表区域                            │
│  │   ├── <Card title="请求量趋势">                                                 │
│  │   │   └── <ResponsiveContainer>                                                │
│  │   │       └── <LineChart data={timeline}>                                       │
│  │   │           ├── <XAxis dataKey="timestamp" />                                │
│  │   │           ├── <YAxis />                                                     │
│  │   │           └── <Line dataKey="requests" />                                   │
│  │   │                                                                              │
│  │   └── <Card title="LLM Token使用">                                             │
│  │       └── <ResponsiveContainer>                                                 │
│  │           └── <LineChart data={llmTimeline}>                                    │
│  │                                                                  │
│  ├── <ServicesStatus />                      服务状态表                           │
│  │   └── <Table>                                                                 │
│  │       ├── <Table.Column name="服务" />                                           │
│  │       ├── <Table.Column name="Span数" />                                        │
│  │       ├── <Table.Column name="错误率" />                                        │
│  │       └── <Table.Column name="P99延迟" />                                       │
│  │                                                                  │
│  └── <CacheSection />                         缓存状态区                           │
│      ├── <Card title="内存缓存(L1)">                                                │
│      │   ├── <Progress percent={...} />                                              │
│      │   └── <Text>命中率: {hitRate}%</Text>                                        │
│      │                                                                              │
│      └── <Card title="Redis缓存(L2)">                                               │
│          ├── <Statistic value={keys} />                                             │
│          └── <Text>命中率: {hitRate}%</Text>                                        │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 缓存管理页面组件

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           缓存管理页面组件结构                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  <CacheManagement />                        缓存管理主组件                           │
│  ├── <Tabs>                                 Tab 切换                                │
│  │   ├── <TabPane key="stats">统计</TabPane>                                        │
│  │   ├── <TabPane key="keys">键管理</TabPane>                                       │
│  │   ├── <TabPane key="invalidate">清理</TabPane>                                    │
│  │   └── <TabPane key="warmup">预热</TabPane>                                       │
│  │                                                                  │
│  │   {/* 统计 Tab */}                                                               │
│  │   <CacheStats />                                                                  │
│  │   ├── <Row>                                                                   │
│  │   │   ├── <Col span={12}><Card>内存缓存统计</Card></Col>                       │
│  │   │   └── <Col span={12}><Card>Redis缓存统计</Card></Col>                      │
│  │   │                                                                              │
│  │   └── <Row>                                                                   │
│  │       ├── <Col span={8}><StatCard title="总命中率" value={...} /></Col>         │
│  │       ├── <Col span={8}><StatCard title="L1命中率" value={...} /></Col>        │
│  │       └── <Col span={8}><StatCard title="L2命中率" value={...} /></Col>        │
│  │                                                                                  │
│  │   {/* 键管理 Tab */}                                                            │
│  │   <CacheKeyManager />                                                           │
│  │   ├── <Input.Search placeholder="搜索键..." onSearch={...} />                 │
│  │   └── <Table>                                                                   │
│  │       ├── <Table.Column key="key" />                                            │
│  │       ├── <Table.Column key="ttl" />                                            │
│  │       └── <Table.Column key="actions" />                                        │
│  │                                                                                  │
│  │   {/* 清理 Tab */}                                                              │
│  │   <CacheInvalidate />                                                           │
│  │   ├── <Button onClick={() => invalidate('user:*')}>清除用户缓存</Button>        │
│  │   ├── <Button onClick={() => invalidate('document:*')}>清除文档缓存</Button>   │
│  │   ├── <Button onClick={() => invalidate('search:*')}>清除搜索缓存</Button>      │
│  │   ├── <Button danger onClick={flushAll}>清空所有缓存</Button>                  │
│  │   └── <ConfirmModal />                                                         │
│  │                                                                                  │
│  │   {/* 预热 Tab */}                                                              │
│  │   <CacheWarmup />                                                               │
│  │   ├── <Radio.Group>                                                             │
│  │   │   ├── <Radio value="all">全部</Radio>                                       │
│  │   │   ├── <Radio value="user">用户数据</Radio>                                  │
│  │   │   ├── <Radio value="document">文档</Radio>                                 │
│  │   │   └── <Radio value="search">搜索</Radio>                                   │
│  │   ├── <Button type="primary" onClick={startWarmup}>开始预热</Button>            │
│  │   └── <WarmupTaskList />                                                        │
│  │       └── <WarmupTaskItem task={task} />                                        │
│  │           ├── <Progress percent={task.progress} />                             │
│  │           └── <StatusTag status={task.status} />                                │
│  │                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🧪 五、测试架构

### 5.1 E2E 测试分层

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              E2E 测试分层架构                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      用户流程测试 (User Flow Tests)                             │   │
│  │                                                                              │   │
│  │  test_user_registration_flow()                                               │   │
│  │  └─► 打开注册页 ──► 填写表单 ──► 提交 ──► 验证成功 ──► 跳转 Dashboard          │   │
│  │                                                                              │   │
│  │  test_document_search_flow()                                                 │   │
│  │  └─► 登录 ──► 进入搜索页 ──► 输入关键词 ──► 执行搜索 ──► 验证结果               │   │
│  │                                                                              │   │
│  │  test_monitoring_dashboard_flow()                                            │   │
│  │  └─► 登录 ──► 进入监控页 ──► 验证指标加载 ──► 验证图表渲染                     │   │
│  │                                                                              │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      API 集成测试 (API Integration Tests)                       │   │
│  │                                                                              │   │
│  │  TestMetricsEndpoints                                                       │   │
│  │  ├── test_metrics_endpoint()           → GET /metrics → 200                  │   │
│  │  ├── test_metrics_summary()            → GET /metrics/summary → 200          │   │
│  │  └── test_dashboard_overview()         → GET /dashboard/overview → 200       │   │
│  │                                                                              │   │
│  │  TestRateLimitEndpoints                                                       │   │
│  │  ├── test_rate_limit_status()         → GET /rate-limit/status → 200        │   │
│  │  ├── test_quota_crud()                 → CRUD /rate-limit/quota/quotas       │   │
│  │  └── test_rules_crud()                 → CRUD /rate-limit/rules/rules       │   │
│  │                                                                              │   │
│  │  TestCacheEndpoints                                                           │   │
│  │  ├── test_cache_stats()               → GET /cache/stats → 200               │   │
│  │  ├── test_cache_invalidate()          → POST /cache/invalidate → 200        │   │
│  │  └── test_cache_warmup()              → POST /cache/warmup → 200             │   │
│  │                                                                              │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                      │                                              │
│  ┌──────────────────────────────────────────────────────────────────────────────┐   │
│  │                      WebSocket 测试 (WebSocket Tests)                          │   │
│  │                                                                              │   │
│  │  TestWebSocketConnection                                                     │   │
│  │  ├── test_websocket_connection()       → 建立连接 ──► 验证 connected         │   │
│  │  ├── test_websocket_task_subscription() → 订阅任务 ──► 接收确认               │   │
│  │  ├── test_websocket_heartbeat()        → 发送 ping ──► 接收 pong             │   │
│  │  └── test_websocket_reconnection()     → 断开 ──► 重连 ──► 验证恢复           │   │
│  │                                                                              │   │
│  │  TestWebSocketMessaging                                                         │   │
│  │  └── test_broadcast_message()         → 发送广播 ──► 接收消息                 │   │
│  │                                                                              │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 六、性能指标

### 6.1 API 性能基准

| 端点 | 方法 | P50 | P95 | P99 | 并发目标 |
|------|------|-----|-----|-----|---------|
| `/metrics` | GET | < 5ms | < 10ms | < 20ms | 1000 req/s |
| `/metrics/summary` | GET | < 50ms | < 100ms | < 200ms | 100 req/s |
| `/dashboard/overview` | GET | < 200ms | < 500ms | < 1000ms | 50 req/s |
| `/traces/list` | GET | < 100ms | < 300ms | < 500ms | 100 req/s |
| `/rate-limit/status` | GET | < 10ms | < 30ms | < 50ms | 500 req/s |
| `/rate-limit/quota/quotas` | CRUD | < 50ms | < 150ms | < 300ms | 100 req/s |
| `/cache/stats` | GET | < 20ms | < 50ms | < 100ms | 200 req/s |
| `/cache/invalidate` | POST | < 100ms | < 300ms | < 500ms | 50 req/s |
| `/cache/warmup` | POST | < 50ms | < 100ms | < 200ms | 10 req/s |

---

## ✅ 七、验收标准

### 7.1 API 功能验收

| API 组 | 端点 | 验收条件 |
|--------|------|---------|
| Metrics | GET /metrics | 返回 Prometheus 格式指标 |
| Metrics | GET /metrics/summary | 返回 JSON 格式指标摘要 |
| Dashboard | GET /dashboard/overview | 返回完整概览数据 |
| Traces | GET /traces/{id} | 返回完整 Trace 信息 |
| Traces | GET /traces/list | 返回 Trace 列表（分页） |
| Rate Limit | GET /rate-limit/status | 返回当前限流状态 |
| Quota | CRUD /rate-limit/quota/quotas | 配额完整 CRUD |
| Rules | CRUD /rate-limit/rules/rules | 规则完整 CRUD |
| Cache | GET /cache/stats | 返回 L1/L2 缓存统计 |
| Cache | POST /cache/invalidate | 按模式/键清理缓存 |
| Cache | POST /cache/warmup | 启动后台预热任务 |

### 7.2 联调验收

| 前端页面 | 验收条件 |
|---------|---------|
| 监控仪表盘 | 正确调用 `/dashboard/*` API，显示图表和统计数据 |
| Trace 查看器 | 正确调用 `/traces/*` API，显示瀑布图 |
| 限流配置 | 正确调用 `/rate-limit/*` API，支持配额和规则管理 |
| 缓存管理 | 正确调用 `/cache/*` API，支持查看、清理、预热 |

### 7.3 E2E 测试验收

| 测试类型 | 验收条件 |
|---------|---------|
| 用户流程 | Playwright 测试通过率 100% |
| API 集成 | pytest 测试用例通过率 100% |
| WebSocket | 连接/订阅/心跳/重连测试通过 |
